{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Capability for the single mobile window",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
